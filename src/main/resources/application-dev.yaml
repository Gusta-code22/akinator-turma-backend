spring:
  application:
    name: akinator-turma-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/akinator
    username: akinator
    password: akinator
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update